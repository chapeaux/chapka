Help me create a commit with proper AI attribution. Look at the staged changes and:

1. Draft a conventional commit message (subject line in imperative mood, body explaining *why*).
2. Append an `Assisted-by: Claude Opus 4.6` trailer at the end of the message.
3. If the project requires DCO sign-off, remind me to add my own `Signed-off-by:` line — do NOT add it yourself.
4. Show me the full proposed commit message for review before running `git commit`.

If the project has a specific trailer format (check CONTRIBUTING.md or similar), use that format instead.
